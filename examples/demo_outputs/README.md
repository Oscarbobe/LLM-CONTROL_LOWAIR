# 演示输出

Ubuntu 交付检查会生成以下结果：

- `data/processed/instructions/map_last_actions.json`：地图规划后的动作序列。
- `data/reports/latest_report.md`：动作、安全校验和仿真结果汇总报告。
- `data/logs/*.jsonl`：实际运行日志。
- `data/raw/audio/*.wav`：语音控制录音文件。

其中 `data/logs/`、`data/raw/audio/`、`data/reports/` 是运行期目录，默认不要求提交生成文件。
