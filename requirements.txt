# Minimal runtime dependencies for the current Swing automation scripts.
# pyparrot is required, but it is often installed from source with `pip install -e .`.
bluepy
untangle
zeroconf
opencv-python
numpy
PyYAML

# Optional dependencies for the natural-language prototype.
SpeechRecognition
jieba
pydantic
networkx
pandas
scipy
rich

# LLM / NLP dependencies (swing-control-llm conda env).
ollama
transformers
openai-whisper
tiktoken
numba
sentencepiece

# PyTorch (GPU) — install via:
#   pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu124
# or via conda:
#   conda install -n swing-control-llm -c pytorch -c nvidia pytorch pytorch-cuda=12.4
# torch
# torchvision
# torchaudio